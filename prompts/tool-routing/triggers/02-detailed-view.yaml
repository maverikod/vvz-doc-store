schema: codex.tool-trigger/v1
id: detailed_view
priority: 70
match:
  intent: known_project_file_inspection
route:
  adapter: ../CODEX_MCP_PROXY_ADAPTER.yaml
  server_id: code-analysis-server-vvz
  help_packs: [../help/cas-detailed-preview.yaml]
first_command: universal_file_preview
hard_rules:
  - Always pass full_text_max_lines explicitly.
  - Use the integer node_ref returned by preview.
  - Detailed project inspection uses CAS first; this does not authorize CAS or proxy editing.
