# Agent entry points

This directory contains prompts for coding agents.

- `claude_project_prompt.md` — primary prompt for Claude.
- `codex_project_prompt.md` — primary prompt for Codex.

Both prompts must be treated as operational instructions. They point agents to HRS and project standards before any implementation work.
